---
date: 2026-05-29
time: "01:10"
run_id: v040-fourth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - Fourth production run of the skill. Second run of the v0.3.1-extended 8-step procedure. Pre-release scaffold check (step 5) ran in ~8s and passed cleanly. Title heuristic correctly fell back to bare `v0.4.0` (CHANGELOG v0.4.0 has only reserved Keep-a-Changelog subheadings: Added, Changed, Notes).
  - **Adoption check:** the v0.3.1 procedure additions are now load-bearing — each release that follows skips zero steps. The framework's "skill-self-improvement loop" is functioning: v0.2.0 surfaced the title bug → v0.3.0 fixed it (and surfaced the destructive-re-cut workflow gap) → v0.3.1 closed it → v0.4.0 used the closed workflow without surprises.
  - Third entry using the `time:` field (release-from-changelog v0.3.1 + task-complete v0.3.1 + this one). Adoption growing slowly.
  - No proposed deltas for the skill itself this run — it worked as designed.
status: applied
---

# release-from-changelog run — v0.4.0 fourth real invocation

## Context

Fourth production use of the skill. Cut v0.4.0 — the release that adds the `design-agent` skill (closes RISKS Risk 8). Uses the v0.3.1-extended 8-step procedure unchanged.

## Inputs

- `version`: `v0.4.0`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.4.0]` heading found.
2. Extracted 22 lines to `/tmp/release-notes-v0.4.0.md`.
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5)** `python3 -m copier copy . /tmp/opos-prerelease-check ... --vcs-ref=HEAD` → exit 0 in ~8s. Cleaned up. No issues.
6. Title derived as bare `v0.4.0` (reserved subheadings only).
7. `gh release create v0.4.0 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.4.0`.
8. Tag verified.
9. (Post-release scaffold check, not part of the skill): `copier copy --vcs-ref v0.4.0 gh:Koroqe/OPOS /tmp/X -d COMPANY_NAME=DesignCo` confirmed the scaffold ships `design-agent/SKILL.md` + `design-agent/PROCESS.md`; `/skills/design-agent` returns 200 in the rendered scaffold.

## Outcome

`success` — all seven success_criteria satisfied including v0.3.1's `prerelease_scaffold_check_passed`.

## Notes

- Clean release cut #2 in a row (v0.3.1 was the first; v0.4.0 is the second). The pre-release scaffold check pattern is now proven on consecutive releases.
- All 8 issues to date will be closed after Slice 8 (#8).
