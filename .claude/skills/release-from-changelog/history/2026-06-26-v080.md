---
date: 2026-06-26
time: "15:50"
run_id: v080-fourteenth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - 14th production invocation. **Second consecutive successful pre-release scaffold check** (v0.7.2 was the first after v0.6.1; v0.7.0 + v0.7.1 had skipped). Slice 4 of v0.7.2 shipped the `pip install --quiet -r requirements-dev.txt` auto-prefix → v0.7.2's release exercised it for the first time → v0.8.0's release exercised it the second time. Pipeline-quality hygiene work continues paying off.
  - Title fell back to bare `v0.8.0` (only reserved Keep-a-Changelog subheadings: Added, Changed, Notes).
  - **Clean release streak extended to 12 in a row** (v0.3.1 → v0.8.0). Pipeline very mature.
  - 19th history entry adopting `time:` — universal convention.
  - **First minor-bump since v0.7.0** (the array-semantics release). v0.7.x ran for 2 patches (CSS fix v0.7.1; pipeline hygiene v0.7.2). v0.8.0 marks the next capability beat: the design-* quartet completes the org-chart-shape primitives.
  - **Plan-critic discipline RE-ESTABLISHED.** First release with a critic round since v0.6.1 (v0.7.x trio all skipped). 12 findings; 2 CRITICAL caught before execution — would have shipped broken charters. The critic round paid for itself.
status: applied
---

# release-from-changelog run — v0.8.0 fourteenth real invocation

## Context

Fourteenth production use. Cuts v0.8.0 — design-subdept skill. The 4th and final org-chart-shape primitive. First minor-bump since v0.7.0.

## Inputs

- `version`: `v0.8.0`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.8.0]` heading found.
2. Extracted 23 lines to `/tmp/release-notes-v0.8.0.md` (Added + Changed + Notes).
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5 with v0.7.2 auto-pip-install prefix):**
   - `pip install --quiet -r requirements-dev.txt` → exit 0. Second consecutive successful auto-install (first was v0.7.2's own release run).
   - `python3 -m copier copy . /tmp/opos-prerelease-v080 -d COMPANY_NAME=PreReleaseCheck --defaults --vcs-ref=HEAD` → exit 0. Pre-release scaffold check passed.
6. Title derived as bare `v0.8.0` (only reserved subheadings).
7. `gh release create v0.8.0 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.8.0`.
8. Tag verified.
9. **(Post-release scaffold check):** confirmed all v0.8.0 deliverables ship:
   - `.claude/skills/design-subdept/{SKILL.md, PROCESS.md, history/}` ✓
   - `shared/templates/SUBDEPT.md.tmpl` ✓
   - `ops-manager.md` owns 7 (including design-subdept) ✓
   - `chief-of-staff.md` "All 21 v0.8.0 skills" ✓
   - `RISKS.md` Risk 8 "FULLY CLOSED in v0.8.0" ✓
   - `README.md` 8th example ("compliance sub-dept under legal") ✓

## Outcome

`success` — all eight success_criteria satisfied INCLUDING v0.3.1's `prerelease_scaffold_check_passed`. Second consecutive release where this success-criterion is satisfied.

## Notes

- 12 consecutive clean releases.
- **Plan-critic discipline RE-ESTABLISHED this release.** First critic round since v0.6.1 (v0.7.x trio all skipped). The critic caught 2 CRITICAL findings BEFORE execution that would have shipped broken sub-dept charters:
  1. `DEPARTMENT.md.tmpl` has depth-2 hardcoded paths that would render BROKEN LINKS from a depth-3 sub-dept charter. Resolved by forking the template to `SUBDEPT.md.tmpl` with depth-3 paths.
  2. The original draft's `<<DEPT_NAME>> = <parent>/<sub>` substitution produced a Roles bullet pointing at a non-existent nested path while the actual lead lives FLAT under the parent's agents folder. Resolved by splitting into two pure-slug tokens (`<<PARENT_DEPT>>` + `<<SUB_NAME>>`).
  - Plus 5 MAJOR findings (collision-check missing `-lead` suffix; agents-dir-may-not-exist; sub-sub-dept ABORT wording; design-department line 137 not enumerated; slash semantics divergence) — all addressed.
  - The critic round paid for itself; an unreviewed v0.8.0 would have shipped broken charters.
- The pipeline-quality work continues to pay off. Slice 4 of v0.7.2 shipped the auto-pip-install prefix; both v0.7.2 and v0.8.0 releases benefited.
- **Slice 6 of this release will be the SECOND task-complete invocation under v0.7.2's Python step-14 fix.** Expected to work on first invocation per the v0.7.2 validation precedent.
- Next release candidates: cross-machine state coordination (Risk 15 deferred half); `flock`-based locking for Risk 30 ORIGINAL surface; cost-aware critic culling for deliberate-decision (Risk 27); pre-summary skill (Risk 28); canonical `framework-reserved-names.md` (now duplicated across 4 skills including design-subdept); depth-3 cascade verification (Risk #6 in v0.8.0 plan); visual regression with headless-browser screenshot diff.
