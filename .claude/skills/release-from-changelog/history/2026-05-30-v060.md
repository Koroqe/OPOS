---
date: 2026-05-30
time: "16:30"
run_id: v060-ninth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - 9th production invocation. 7th run of the v0.3.1-extended 8-step procedure. Pre-release scaffold check (step 5) ran in ~5s and passed.
  - Title fell back to bare `v0.6.0` (only reserved Keep-a-Changelog subheadings: Added, Changed, Notes).
  - **Clean release streak extended to 7 in a row** (v0.3.1, v0.4.0, v0.5.0, v0.5.1, v0.5.2, v0.5.3, v0.6.0). Pipeline mature; plan-critic + pre-release scaffold check load-bearing for 7 consecutive releases.
  - 14th history entry adopting `time:` — universal convention.
  - **Meta-significance:** v0.6.0 is the autonomy-capable inflection point — first release where OPOS can run unattended. ops-manager now owns 6 skills (3 meta-design + 3 meta-scheduling) and is the framework's single owner of both self-extension axes (what processes exist + when they run).
  - **First minor-version bump since v0.5.0** (the AI-first kernel release). v0.5.x ran for 5 patch releases (v0.5.1 through v0.5.3 + the kb-curator dogfood). v0.6.0 marks the next architectural beat: cron-fired autonomy.
  - **New convention this release:** pre-execution tool-name validation. The brainstorm plan named Claude Code's cron tools wrong (`create_scheduled_task` etc.); validation via the claude-code-guide agent caught it before any wrapper SKILL.md was written. Future releases that depend on external tool/API names should follow the same pattern.
status: applied
---

# release-from-changelog run — v0.6.0 ninth real invocation

## Context

Ninth production use. Cuts v0.6.0 — Scheduled processes mechanism (3 new ops-manager-owned skills wrapping Claude Code's CronCreate / CronList / CronDelete). First release where OPOS can run unattended.

## Inputs

- `version`: `v0.6.0`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.6.0]` heading found.
2. Extracted 31 lines to `/tmp/release-notes-v0.6.0.md` (Added + Changed + Notes; no Migration sub-section — additive only).
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5)** `python3 -m copier copy . /tmp/opos-prerelease-v060 ... --vcs-ref=HEAD` → exit 0 in ~5s. Cleaned up.
6. Title derived as bare `v0.6.0` (only reserved subheadings).
7. `gh release create v0.6.0 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.6.0`.
8. Tag verified.
9. **(Post-release scaffold check):** `copier copy --vcs-ref v0.6.0 gh:Koroqe/OPOS /tmp/X -d COMPANY_NAME=Test --defaults` confirmed all v0.6.0 deliverables ship:
   - `.claude/skills/{schedule-process, unschedule-process, list-scheduled-processes}/` each with SKILL.md + PROCESS.md + history/.gitkeep + scheduled-runs/.gitkeep
   - `shared/templates/scheduled-run.md.tmpl` present
   - `ui/scheduling.py` present
   - `ops-manager.md` `owns_processes` = 6 (all 3 design-* + all 3 scheduling-*)
   - `PROCESS.md.tmpl` has the 4 commented-out scheduling frontmatter fields
   - `README.md` has the new "Scheduling processes" section
   - NO `scheduled-runs/202*.md` files; NO `.claude/scheduled-processes.json`

## Outcome

`success` — all seven success_criteria satisfied including v0.3.1's `prerelease_scaffold_check_passed`.

## Notes

- 7 consecutive clean releases (v0.3.1, v0.4.0, v0.5.0, v0.5.1, v0.5.2, v0.5.3, v0.6.0). Pipeline very mature; the plan-critic step + pre-release scaffold check + (new this release) pre-execution tool-name validation form the 3 load-bearing release-discipline pillars.
- v0.6.0 is the first MINOR-version bump since v0.5.0. The v0.5.x series ran for 4 patch releases (v0.5.1, v0.5.2, v0.5.3 + kb-curator) before this beat.
- The autonomy-capable inflection: any process with the 4 scheduling frontmatter fields can now fire on a cron schedule via Claude Code's native cron runtime. OPOS is no longer request-driven only.
- Plan-critic load-bearing for 6 consecutive releases (v0.4.0 through v0.6.0).
- Next release candidates (deferred from v0.6.0): `design-subdept` (sub-depts under existing depts), `data/` + `backlog/` backfill on 5 starters, canonical `framework-reserved-names.md`, post-run sandbox guard for scheduling (v2 work item for authority enforcement), `health-check` skill for silent-cron-failure detection.
