---
date: 2026-05-30
time: "00:30"
run_id: v051-sixth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - 6th production invocation. 4th run of the v0.3.1-extended 8-step procedure. Pre-release scaffold check (step 5) ran in ~10s and passed — first time the scaffold check has exercised a MAJOR restructure (engineering folder removal + 5 new dept folders + .claude/agents/engineering removal). The check's value is now empirically proven on a high-risk change.
  - **First release shipping a SPECIFIC organizational philosophy as starter content.** Prior 5 releases (v0.3.1, v0.4.0, v0.5.0) shipped primitives; v0.5.1 ships the AI-first kernel via allocate-resource + the 6-dept structure. Adopters now START with an opinionated org chart instead of needing to design one.
  - Title fell back to bare `v0.5.1` (only reserved Keep-a-Changelog subheadings: Added, Changed, Removed, Migration, Notes). First release in framework history to ship a `### Migration` subsection (per Risk 13 convention).
  - Clean release streak: v0.3.1, v0.4.0, v0.5.0, v0.5.1 — 4 in a row with no destructive recovery. The plan-critic step + the pre-release scaffold check are now load-bearing framework infrastructure.
  - 9th history entry adopting `time:` — universal convention.
status: applied
---

# release-from-changelog run — v0.5.1 sixth real invocation

## Context

Sixth production use. Cuts v0.5.1 — the most opinionated release yet (ships a specific AI-first organizational philosophy as starter content). First framework release to ship a `### Migration` CHANGELOG subsection.

## Inputs

- `version`: `v0.5.1`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.5.1]` heading found.
2. Extracted 68 lines to `/tmp/release-notes-v0.5.1.md` (largest CHANGELOG entry to date — Added + Changed + Removed + Migration + Notes).
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5 — first exercised on a major restructure)** `python3 -m copier copy . /tmp/opos-prerelease-check ... --vcs-ref=HEAD` → exit 0 in ~10s. Cleaned up. **The scaffold check now has empirically-validated value: it caught nothing this run (nothing to catch), but the fact that a 6-dept restructure + agent moves + skill moves passes the scaffold gate cleanly is itself the signal.**
6. Title derived as bare `v0.5.1` (only reserved subheadings).
7. `gh release create v0.5.1 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.5.1`.
8. Tag verified.
9. **(Post-release scaffold check — not part of the skill but executed for the AI-first kernel release):** `copier copy --vcs-ref v0.5.1 gh:Koroqe/OPOS /tmp/X -d COMPANY_NAME=AiFirstCo` confirmed:
   - 6 dept folders: commercial, finance, legal, people, pr, rnd (NO engineering at top-level — correctly absorbed)
   - 5 new lead agents present
   - allocate-resource SKILL.md + DEPARTMENT.md.tmpl + HIRING-SPEC.md.tmpl + company/hiring/README.md all ship
   - Console smoke: `/skills/allocate-resource` → 200, `/agents/people/people-lead` → 200, `/departments/finance` → 200, `/agents/rnd/eng-lead` → 200 (verifies the merge worked)

## Outcome

`success` — all seven success_criteria satisfied including v0.3.1's `prerelease_scaffold_check_passed`.

## Notes

- This is the FIRST framework release where the underlying philosophy is the deliverable, not just primitives. Adopters scaffolding from v0.5.1 immediately get the 6-dept AI-first structure; the `/company-setup` flow walks them through customizing it.
- Clean release streak extended: 4 in a row (v0.3.1, v0.4.0, v0.5.0, v0.5.1). The release pipeline is mature.
- The user's `zipread` company can now be scaffolded from v0.5.1 and will get the 7-dept structure + AI-first kernel from day 1 (no manual restructure needed).
