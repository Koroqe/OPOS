---
date: 2026-05-31
time: "15:15"
run_id: v061-tenth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - 10th production invocation. 8th run of the v0.3.1-extended 8-step procedure. Pre-release scaffold check (step 5) ran in ~5s and passed.
  - Title fell back to bare `v0.6.1` (only reserved Keep-a-Changelog subheadings: Added, Changed, Notes).
  - **Clean release streak extended to 8 in a row** (v0.3.1, v0.4.0, v0.5.0, v0.5.1, v0.5.2, v0.5.3, v0.6.0, v0.6.1). Pipeline very mature.
  - 15th history entry adopting `time:` — universal convention.
  - **Meta-significance:** v0.6.1 adds the framework's missing agent-to-agent critique loop — `deliberate-decision`. The propose→critique→revise pattern that's been load-bearing on 7 OPOS releases via plan-critic is now a first-class company process.
  - **Plan went through TWO review layers** (plan-critic 27 findings + post-ExitPlanMode pressure-test 8 findings) — both surfaced load-bearing flaws (ESCALATION dict wrong for 4 of 6 dept-leads; direct-Task-vs-consult-agent middleware ambiguity). New convention candidate this release: post-ExitPlanMode pressure-test for skills orchestrating multi-step subagent work.
  - **Bonus fix shipped this release:** `task-complete` SKILL step 13 `mv` → `git mv` closes the v0.5.3 / v0.6.0 dual-tracking bug at root. Slice 7 will be the first task-complete run to exercise the new path.
status: applied
---

# release-from-changelog run — v0.6.1 tenth real invocation

## Context

Tenth production use. Cuts v0.6.1 — `deliberate-decision` skill (multi-round propose/critique/revise loop). First patch under the v0.6.x autonomy-capable line; adds the framework's missing agent-to-agent critique loop on top of v0.6.0's scheduling infrastructure.

## Inputs

- `version`: `v0.6.1`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.6.1]` heading found.
2. Extracted 30 lines to `/tmp/release-notes-v0.6.1.md` (Added + Changed + Notes).
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5)** `python3 -m copier copy --vcs-ref HEAD . /tmp/opos-prerelease-v061 ...` → exit 0 in ~5s.
6. Title derived as bare `v0.6.1` (only reserved subheadings).
7. `gh release create v0.6.1 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.6.1`.
8. Tag verified.
9. **(Post-release scaffold check):** `copier copy --vcs-ref v0.6.1 gh:Koroqe/OPOS /tmp/X ...` confirmed:
   - `.claude/skills/deliberate-decision/{SKILL.md, PROCESS.md, history}` present
   - `shared/templates/decision.md.tmpl` present
   - `company/decisions/{README.md, .gitkeep}` present (folder ships as CORE — NOT _skip_if_exists)
   - `coo.md` `owns_processes` = `['company-setup', 'deliberate-decision']`
   - `task-complete` SKILL.md uses `git mv` (bonus fix shipped)
   - README has Berlin example
   - chief-of-staff reflects "All 20 v0.6.1 skills" (v0.6.0 backfill + v0.6.1)
   - Risk 27 in RISKS.md
   - NO `company/decisions/202*.md` (runtime-only files correctly excluded)

## Outcome

`success` — all seven success_criteria satisfied including v0.3.1's `prerelease_scaffold_check_passed`.

## Notes

- 8 consecutive clean releases. Pipeline very mature; plan-critic + pre-release scaffold check + pre-execution external-API validation + post-ExitPlanMode pressure-test now 4 load-bearing release-discipline layers.
- v0.6.1 plan critic round: 27 findings (4 critical, 15 major, 8 minor); all CRITICAL/MAJOR addressed. Pressure-test round: 8 findings; all addressed. The recursion is intentional — the first release of `deliberate-decision` is itself deliberated by the framework's own critique primitive applied to its own design.
- **Bonus fix** in this release: `task-complete` SKILL.md `mv` → `git mv`. Closes the dual-tracking bug that the v0.5.3 / v0.6.0 task-complete runs hit. Cleanup commit `b775b0f` was retroactive; this fix is at root. Slice 7 (this release's task-complete run) will be the first invocation under the new path.
- Next release candidates: post-run sandbox guard for scheduling Risk 18 (v2 work item); `health-check` skill for silent-cron-failure detection (Risk 20); `design-subdept`; data/+backlog/ backfill on 5 starters; canonical `framework-reserved-names.md`; cost-aware critic culling for `deliberate-decision` (Risk 27 mitigation).
