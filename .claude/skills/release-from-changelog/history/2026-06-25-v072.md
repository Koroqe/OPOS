---
date: 2026-06-25
time: "11:30"
run_id: v072-thirteenth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - 13th production invocation. **First release in v0.7.x line that RAN the pre-release scaffold check successfully** — was skipped in v0.7.0 + v0.7.1 due to copier env regressions. Slice 4 of this same release shipped the `pip install --quiet -r requirements-dev.txt` prefix to step 5; that prefix ran cleanly (pip exit 0) and the subsequent `copier copy ... --vcs-ref=HEAD` exited 0. The pipeline-quality hygiene fix demonstrated its value on its own release.
  - Title fell back to bare `v0.7.2` (only reserved Keep-a-Changelog subheadings: Added, Fixed, Changed, Notes).
  - **Clean release streak extended to 11 in a row** (v0.3.1 → v0.7.2). Pipeline very mature.
  - 18th history entry adopting `time:` — universal convention.
  - **Pipeline meta-observation:** v0.7.2 is the first release where a release-pipeline mechanism (Slice 4's auto-pip-install) directly fixed a release-pipeline failure mode (scaffold check skipped due to broken copier env) IN THE SAME RUN. Self-improving framework in action.
status: applied
---

# release-from-changelog run — v0.7.2 thirteenth real invocation

## Context

Thirteenth production use. Cuts v0.7.2 — pipeline hygiene bundle. **First release in v0.7.x line where the pre-release scaffold check ran successfully** (was skipped in v0.7.0 + v0.7.1 due to `copier` env regression). Slice 4 of this same release shipped the auto-pip-install prefix that made this run possible.

## Inputs

- `version`: `v0.7.2`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.7.2]` heading found.
2. Extracted 24 lines to `/tmp/release-notes-v0.7.2.md` (Added + Fixed + Changed + Notes).
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5, with v0.7.2 auto-pip-install prefix):**
   - `pip install --quiet -r requirements-dev.txt` → exit 0. **First time this step runs in production**.
   - `python3 -m copier copy . /tmp/opos-prerelease-v072 -d COMPANY_NAME=PreReleaseCheck --defaults --vcs-ref=HEAD` → exit 0. Cleaned up.
   - **This is the FIRST successful scaffold check in v0.7.x** — was skipped for v0.7.0 + v0.7.1 (copier was broken on this machine; the `requirements-dev.txt` introduced by Slice 1 + auto-pip-install introduced by Slice 4 fixed it at root).
6. Title derived as bare `v0.7.2` (only reserved subheadings).
7. `gh release create v0.7.2 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.7.2`.
8. Tag verified.
9. **(Post-release scaffold check):** `copier copy --vcs-ref v0.7.2 gh:Koroqe/OPOS /tmp/X -d COMPANY_NAME=Test --defaults` confirmed all v0.7.2 deliverables ship:
   - `requirements-dev.txt` present ✓
   - `task-complete/SKILL.md` uses `python3 -c` Python one-liner ✓
   - `task-pause/SKILL.md` uses `python3 -c` Python one-liner ✓
   - `task-complete/SKILL.md` retains v0.6.1 `git mv` for archive ✓
   - `release-from-changelog/SKILL.md` has 2 references to `pip install --quiet -r` ✓
   - `RISKS.md` has Risk 30 v0.7.2 status update (Step-14 ELIMINATED AT ROOT) ✓
   - **`MAINTAINER.md` + `ui/smoke.sh` correctly NOT shipped** (framework-only files; in `copier.yml` `_exclude`) — checking these in the post-release scaffold tree correctly returns "no such file" warnings.

## Outcome

`success` — all eight success_criteria satisfied INCLUDING v0.3.1's `prerelease_scaffold_check_passed` (which had been failing for 2 consecutive releases).

## Notes

- 11 consecutive clean releases. Pipeline very mature.
- **Self-improving framework in action:** v0.7.2's Slice 4 shipped the auto-pip-install fix; v0.7.2's Slice 6 (THIS release-from-changelog run) was the first to benefit from that fix; it ran cleanly. The release-pipeline hygiene work directly fixed the release-pipeline's biggest recurring failure mode in the same release.
- `prerelease_scaffold_check_passed` is back to a passing state after 2 releases (v0.7.0 + v0.7.1) of being skipped + replaced with `git show` fallback verification.
- Slice 7 of this release is the **validating exercise** of the Python step-14 fix. If `task-complete` works on FIRST invocation under the new logic, the third pipeline-quality issue (reproducibly-flaky shell chain) is also resolved at root.
- Next release candidates: cross-machine state coordination (Risk 15 deferred half); `flock`-based locking around `.current-task` `>>` append (Risk 30 ORIGINAL surface mitigation); cost-aware critic culling for deliberate-decision (Risk 27); pre-summary skill for long deliberations (Risk 28); `design-subdept`; canonical `framework-reserved-names.md`; visual regression with headless-browser screenshot diff (extends Slice 3's smoke assertions).
