---
date: 2026-05-29
time: "00:30"
run_id: v031-third-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - **First invocation of the v0.3.1-EXTENDED 8-step procedure** (added step 5: pre-release scaffold check). The new step ran a fresh `copier copy . /tmp/opos-prerelease-check --vcs-ref=HEAD` and exited 0 in ~8s. No surprises. The workflow gap from v0.3.0 (delete-and-re-cut after discovering the `.html.jinja` issue) is now closed.
  - **First entry to use the new optional `time:` field** introduced in this very release (recursive dogfood). Worked.
  - Title heuristic: v0.3.1 entry has only `### Fixed`, `### Changed`, `### Notes` — all reserved. Title fell back to bare `v0.3.1`, exactly as designed.
  - Self-validation chain extended: each release validates the previous release's fixes AND the current release's additions. v0.3.0 fixed v0.2.0's title bug. v0.3.1 fixes v0.3.0's dept-nested-skill blindspot AND adds the pre-release check that v0.3.0 needed.
status: applied
---

# release-from-changelog run — v0.3.1 third real invocation

## Context

Third production use of the skill. Cut v0.3.1 of OPOS — the patch that addresses the v0.3.0 console UX walkthrough findings (parse_skills dept-nested, dashboard polish, activity time sort) and folds in the v0.3.0 release-process carryover (pre-release scaffold check) into this very skill. Recursive self-test.

## Inputs

- `version`: `v0.3.1`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.3.1]` heading found.
2. Extracted 20 lines to `/tmp/release-notes-v0.3.1.md`.
3. Non-empty.
4. No existing release.
5. **(NEW)** `python3 -m copier copy . /tmp/opos-prerelease-check ... --vcs-ref=HEAD` → exit 0 in ~8s. Cleaned up `/tmp/opos-prerelease-check`. The v0.3.0 lesson applied: catch Copier-side breakage BEFORE tagging.
6. Title derived as bare `v0.3.1` (only reserved subheadings).
7. `gh release create v0.3.1 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.3.1`.
8. Tag verified.
9. (Post-release scaffold check, not part of the skill): `copier copy --vcs-ref v0.3.1 gh:Koroqe/OPOS /tmp/X` confirmed `/skills/deploy` returns 200 + total skills = 12 in the rendered scaffold.

## Outcome

`success` — all seven success_criteria (including the new `prerelease_scaffold_check_passed`) satisfied.

## Notes

- The pre-release check added ~8 seconds to the release process. Worth it.
- This is the first entry to include the optional `time:` field. Future entries should adopt the convention.
