---
date: 2026-05-29
time: "02:00"
run_id: v050-fifth-real-invocation
skill: release-from-changelog
actor: chief-of-staff
outcome: success
duration_min: 1
proposed_delta: |
  - Fifth production run. Third run of the v0.3.1-extended 8-step procedure. Pre-release scaffold check (step 5) ran in ~8s and passed. Title fell back to bare `v0.5.0` (entry has only reserved Keep-a-Changelog subheadings: Added, Changed, Notes).
  - **First release of a CONSUMER-FACING skill.** Prior 4 releases shipped framework-internal additions; v0.5.0 ships company-setup which the founder runs. Post-release scaffold (`copier copy --vcs-ref v0.5.0 ... -d COMPANY_NAME=FounderCo`) confirmed company-setup is discoverable in the consumer's repo + coo's owned-skills shows it.
  - **No skill self-modifications this release** — release-from-changelog SKILL.md was unchanged. The procedure stabilized at v0.3.1. Streak of clean releases: v0.3.1, v0.4.0, v0.5.0 (3 in a row with no destructive recovery).
  - 6th history entry adopting `time:` — convention fully adopted across release/task-complete entries.
status: applied
---

# release-from-changelog run — v0.5.0 fifth real invocation

## Context

Fifth production use. Cuts v0.5.0 — the first consumer-facing release (company-setup skill for founder onboarding). Uses the v0.3.1-extended 8-step procedure unchanged.

## Inputs

- `version`: `v0.5.0`
- `target_branch`: `feat/company-os-framework`
- `repo`: `Koroqe/OPOS`

## What happened

1. `## [0.5.0]` heading found.
2. Extracted 22 lines to `/tmp/release-notes-v0.5.0.md`.
3. Non-empty.
4. No existing release.
5. **(v0.3.1 step 5)** `python3 -m copier copy . /tmp/opos-prerelease-check ... --vcs-ref=HEAD` → exit 0 in ~8s. Cleaned up.
6. Title derived as bare `v0.5.0` (only reserved subheadings).
7. `gh release create v0.5.0 ...` → `https://github.com/Koroqe/OPOS/releases/tag/v0.5.0`.
8. Tag verified.
9. (Post-release scaffold check, not part of the skill): `copier copy --vcs-ref v0.5.0 gh:Koroqe/OPOS /tmp/X -d COMPANY_NAME=FounderCo` confirmed `.claude/skills/company-setup/SKILL.md` + `PROCESS.md` + `shared/templates/POLICY.md.tmpl` all ship; `/skills/company-setup` returns 200; `/agents/company/coo` lists company-setup in owned-skills.

## Outcome

`success` — all seven success_criteria satisfied.

## Notes

- **First release to be cut against the v0.5.0 copier.yml._skip_if_exists change for root CLAUDE.md.** Consumer scaffolds from this version forward will have founder-owned root CLAUDE.md (Mission/Values survive `copier update`).
- Clean release streak: 3 in a row (v0.3.1, v0.4.0, v0.5.0). The pre-release scaffold check pattern is now load-bearing infrastructure.
- The user's `zipread` company setup is the immediate next-real-world-use; that run's `proposed_delta` will drive v0.5.1.
