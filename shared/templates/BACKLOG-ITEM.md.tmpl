<!--
TEMPLATE: substitute the tokens listed below.

Substitution tokens:
  <<ITEM_TITLE>>         — short title describing the experiment / one-off task
  <<OWNER_AGENT>>        — agent name responsible for running this item and (when ready) feeding it to design-process
  <<CREATED_DATE>>       — ISO date (YYYY-MM-DD) when the item was filed
  <<INTENDED_TARGET>>    — intended skill path if/when this item informs a process design (e.g. "departments/engineering/.claude/skills/<new-skill>/")
-->

---
title: <<ITEM_TITLE>>
owner: <<OWNER_AGENT>>
created: <<CREATED_DATE>>
state: proposed
runs: 0
intended_target: <<INTENDED_TARGET>>
---

# <<ITEM_TITLE>>

## Goal

What outcome this backlog item is trying to produce. One short paragraph.

## Acceptance

How we know a single run succeeded — concrete, observable criteria.

## Runs log

An audit trail of when this item was executed manually; informational only. When the owner decides this item is ready to formalize as a recurring process, they invoke the `design-process` skill (owned by `ops-manager`) — `design-process` is the path to formalization.

| date | actor | outcome | notes |
|------|-------|---------|-------|
