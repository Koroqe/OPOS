# {{ COMPANY_NAME }} — Company OS (Constitution)

This is the root CLAUDE.md for {{ COMPANY_NAME }}. It is **yours** — Copier
never overwrites it, so anything you write here survives every framework
update. Keep it short: every line loads into context on every call.

Framework mechanics — the steward posture, operating principles, global rules,
the self-improvement log schema — live in
[`.claude/CLAUDE.md`](.claude/CLAUDE.md), which is framework-owned and updates
automatically. Don't restate them here; if the two ever disagree,
`.claude/CLAUDE.md` is newer and wins (it loads after this file).

## Mission

{{ COMPANY_NAME }} exists to <one short sentence>. The company is operated by
humans and AI agents working from this repository.

## Values

- Value 1 — one-line restatement.
- Value 2 — one-line restatement.
- Value 3 — one-line restatement.
- Value 4 — one-line restatement.
- Value 5 — one-line restatement.

## How to work here

Open Claude Code at the repo root and state a goal in plain language. The
session runs as your **chief-of-staff** — the OPOS steward — which decomposes
the goal, invokes the right skills and agents itself, handles git, and asks
permission only at the gates. You do not need to invoke `/anything`.

Everything the steward knows and every gate it honors:
[`.claude/agents/company/chief-of-staff.md`](.claude/agents/company/chief-of-staff.md).

## Company-specific rules

Add rules that apply to {{ COMPANY_NAME }} specifically — domain constraints,
regulatory obligations, house style, anything an agent should treat as binding.
Framework-wide rules are already covered in `.claude/CLAUDE.md`.

- (none yet)

## Glossary

See `company/knowledge-base/glossary.md`.
