---
issue_number: 18
title: v0.8.0 — design-subdept skill (4th + final org-chart-shape primitive)
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-06-26
completed: 2026-06-26
success_criteria:
  - design-subdept skill (SKILL.md + PROCESS.md + history/.gitkeep) shipped with 12-step procedure mirroring design-department
  - SUBDEPT.md.tmpl shipped (NEW; forked from DEPARTMENT.md.tmpl with depth-3 paths + new PARENT_DEPT token; 7 tokens total)
  - ops-manager owns_processes 6 → 7; Role narrative paragraph + Owned-processes bullet
  - design-department/SKILL.md 4 places updated (line 21 + step 4 + failure-modes line 137 + Related)
  - chief-of-staff "All 20 v0.6.1 skills" → "All 21 v0.8.0 skills" with design-subdept marker
  - README 8th day-to-day example
  - RISKS Risk 8 title + status + resolution paragraph (third + final tier closed)
  - CHANGELOG ## [0.8.0] + link reference
  - v0.8.0 GitHub release tag
  - 53/53 unittest + 24/24 smoke PASS
  - Issue #18 CLOSED with status:done; tasks/18.md archived via git mv
  - THIRD validation of v0.7.2 Python step-14 fix on first invocation
deadline:
related_skills: [task-register, task-complete, release-from-changelog, design-department, design-subdept]
---

# v0.8.0 — design-subdept skill (4th + final org-chart-shape primitive)

## Goal

Ship the LAST org-chart-shape primitive. Completes the design-* family of 4 skills under ops-manager. Closes RISKS Risk 8 fully-fully (third tier — v0.4.0 closed agents, v0.5.3 closed top-level depts, v0.8.0 closes sub-depts).

Structural model: real folder nesting at `departments/<parent>/<sub>/`. Charter cascade-inherits from parent + root. Sub-lead recommended via `/design-agent` emit-not-invoke pattern. Forked template (`SUBDEPT.md.tmpl`) with depth-3 paths.

Plan-critic re-established for v0.8.0 after 3-release skip. 12 findings (2 CRITICAL, 5 MAJOR, 5 MINOR); all addressed.

## Acceptance criteria

See issue body's "Acceptance criteria" section (15 checks including CRITICAL VALIDATION at Slice 6).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

**Shipped:** v0.8.0 — `design-subdept` skill, the 4th and final org-chart-shape primitive. Completes the design-* quartet under ops-manager. Closes RISKS Risk 8 fully-fully (third + final tier). Released at https://github.com/Koroqe/OPOS/releases/tag/v0.8.0. Issue closed via task-complete; final comment at https://github.com/Koroqe/OPOS/issues/18#issuecomment-4810547568.

### 🎉 CRITICAL VALIDATION — Python step-14 fix worked on FIRST INVOCATION (2nd consecutive)

Trace:
- Before: `.current-task` = `18` (single-element array)
- Python exit: 0
- After: `.current-task` ABSENT (single-element → `lines = []` → `os.remove()` fired)

**The v0.7.2 Python one-liner replacement continues to validate.** v0.7.2's own Slice 7 was the first successful run; this is the SECOND. The old flaky `grep -v ... && mv ...` shell chain that failed first-try across v0.7.0 + v0.7.1 is firmly behind us.

### Plan-critic discipline RE-ESTABLISHED

After a 3-release skip (v0.7.0 + v0.7.1 + v0.7.2 — judgment call for small-surface hygiene work), v0.8.0 ran plan-critic. **12 findings:** 2 CRITICAL + 5 MAJOR + 5 MINOR.

**The 2 CRITICALs would have shipped broken sub-dept charters:**
1. `DEPARTMENT.md.tmpl`'s depth-2 hardcoded paths would have rendered BROKEN LINKS from a depth-3 sub-dept charter. Resolved by forking the template to `SUBDEPT.md.tmpl` with depth-3 paths + new `<<PARENT_DEPT>>` token.
2. `<<DEPT_NAME>> = <parent>/<sub>` substitution semantics conflicted with the flat sub-lead agent placement (would have rendered link at `.claude/agents/legal/compliance/compliance-lead.md` while the actual lead lives FLAT at `.claude/agents/legal/compliance-lead.md`). Resolved by splitting into two pure-slug tokens.

**The critic round paid for itself.** Worth maintaining for v0.8.x+ substantive work.

### Self-improving framework continues

v0.8.0 is the **second consecutive release benefiting from v0.7.2 Slice 4's auto-pip-install prefix** in `release-from-changelog` step 5. The pre-release scaffold check ran successfully (was skipped v0.7.0 + v0.7.1). The pipeline-quality work compounds.

### Commits

6 v0.8.0 commits, all `Refs: #18` (100% adoption — 18 consecutive releases at 100%). v0.6.1 `git mv` fix still effective at step 13 (clean rename).

### Verification

- Pre-release scaffold check ran cleanly (2nd consecutive success).
- Post-release scaffold ships all v0.8.0 deliverables.
- 53/53 unittest + **24/24 smoke** PASS.

### Framework state after v0.8.0

The org-chart-shape design family is COMPLETE. ops-manager owns the 4-skill quartet:

| Release | Skill | Closes |
|---|---|---|
| v0.1.0 | design-process | new skills |
| v0.4.0 | design-agent | new agent roles |
| v0.5.3 | design-department | new top-level depts |
| **v0.8.0** | **design-subdept** | **new sub-depts** |

Plus 3 meta-scheduling skills (schedule-process / unschedule-process / list-scheduled-processes) = 7 owned skills total. ops-manager is the framework's complete org-chart-expansion + scheduling controller.

Plan-critic load-bearing count now: **7 consecutive releases** (v0.4.0 → v0.6.1 + v0.8.0). v0.7.x trio skipped (acknowledged).

### Deviations from plan

None substantive. The plan's design held under execution; the critic-resolved fixes worked as designed.

### Next likely candidates

- Cross-machine state coordination (Risk 15 deferred half)
- `flock`-based locking around `.current-task` `>>` append (Risk 30 ORIGINAL surface)
- Cost-aware critic culling for `deliberate-decision` (Risk 27)
- Pre-summary skill for long deliberations (Risk 28)
- **Canonical `framework-reserved-names.md`** — now duplicated across 4 skills (company-setup, design-agent, design-department, design-subdept); consolidation overdue
- **Depth-3 cascade verification** (v0.8.0 plan Risk #6) — sub-dept cascade is documented but not empirically verified
- Visual regression with headless-browser screenshot diff (extends v0.7.2's CSS rule checks)
