---
issue_number: 14
title: v0.6.1 — deliberate-decision skill (multi-round propose/critique/revise loop)
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-05-31
completed: 2026-05-31
success_criteria:
  - deliberate-decision skill present with verbatim ESCALATION dict (eng-lead → rnd-lead; rnd-lead/finance-lead → coo; people/legal/commercial/pr-lead → ceo; coo → ceo; ceo → None) + STANCE parser + direct Task-call pattern + tmp-file persistence + UUID decision_id + mkdir -p before write
  - decision.md.tmpl shipped with 10-field frontmatter
  - company/decisions/{README.md, .gitkeep} shipped as CORE (NOT in _skip_if_exists)
  - copier.yml _exclude has `company/decisions/202[0-9]-*.md`
  - coo.md owns_processes 1 → 2 (company-setup + deliberate-decision); body updated
  - README.md.jinja "How to use OPOS day-to-day" 6th example (Berlin) + cost note
  - chief-of-staff.md Framework expertise "All 20 v0.6.1 skills" (v0.6.0 backfill + v0.6.1)
  - RISKS Risk 27 (deliberation cost burn)
  - CHANGELOG ## [0.6.1] entry + link reference
  - task-complete SKILL step 13 mv → git mv (bonus fix; closes v0.5.3/v0.6.0 dual-tracking bug at root)
  - v0.6.1 release tag exists; post-release scaffold ships all v0.6.1 deliverables
  - 53/53 unittest PASS; 16/16 smoke PASS
  - issue #14 CLOSED with status:done; tasks/14.md archived to tasks/closed/14.md via git mv; .current-task absent
deadline:
related_skills: [task-register, task-update, task-complete, design-process, release-from-changelog]
---

# v0.6.1 — deliberate-decision skill (multi-round propose/critique/revise loop)

## Goal

Ship OPOS v0.6.1: the `deliberate-decision` skill — a multi-round propose → critique → revise loop for high-level company decisions. Owner: coo. Orchestrates parallel direct Task calls (NOT through consult-agent middleware) against all 6 dept-leads + escalation-target as critics; proposer revises between rounds with critic-memory across rounds; arbiter renders APPROVE/REJECT/DEFER; human approves at step 12 (human-in-the-loop). Decision artifact written to new `company/decisions/` folder. Bonus: closes v0.5.3/v0.6.0 `mv`-vs-`git mv` dual-tracking bug at root.

## Acceptance criteria

See issue body's "Acceptance criteria" section (14 checks).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

**Shipped:** v0.6.1 — `deliberate-decision` skill (multi-round propose/critique/revise loop). Framework gains agent-to-agent deliberation; coo now owns 2 skills (company-setup + deliberate-decision). Released at https://github.com/Koroqe/OPOS/releases/tag/v0.6.1. Issue closed via task-complete; final comment at https://github.com/Koroqe/OPOS/issues/14#issuecomment-4586914826.

**Bonus shipped:** `task-complete` SKILL step 13 `mv` → `git mv` (closes v0.5.3 / v0.6.0 dual-tracking bug at root). THIS task-complete run is the first to exercise the new path; `git status` shows clean `R tasks/14.md -> tasks/closed/14.md` rename (deletion staged atomically with the add — no orphan files).

**Commits:** 6 v0.6.1 commits in v0.6.0..HEAD range, all with `Refs: #14` (100% adoption — 14 consecutive releases at 100% adoption when counting from when the convention was introduced). One additional commit in the range (`b775b0f`) is the v0.6.0 retroactive cleanup that predates v0.6.1 work and correctly omits the trailer.

**Verification:**
- Pre-release scaffold check (v0.3.1 step 5): exit 0 in ~5s — 8 consecutive clean releases.
- Post-release scaffold ships all v0.6.1 deliverables: deliberate-decision skill, decision.md.tmpl, company/decisions/{README, .gitkeep}, coo with 2 owns_processes, task-complete using `git mv`, README Berlin example, chief-of-staff 20 skills, Risk 27.
- 53/53 unittest + 16/16 smoke PASS.
- All 14 acceptance criteria met.

**Plan critic + pressure-test:** Plan went through TWO review layers. Plan-critic surfaced 27 findings (4 critical, 15 major, 8 minor) — most consequential were CRITICAL escalation-dict-wrong-for-4-of-6-dept-leads + `eng-lead`-missing-from-dict. Post-ExitPlanMode pressure-test surfaced 8 architectural findings about Claude Code subagent mechanics — most consequential was direct-Task-vs-consult-agent-middleware ambiguity (would have caused execution failure since `consult-agent` is a skill, not an agent). Both review layers shipped load-bearing fixes.

**Deviations from plan:** Slice 2 copier render had to be moved post-commit (Copier reads from git tree, not working tree). Documented in execution; no impact on final deliverables.

**Framework state after v0.6.1:** 3 meta-skill families now operational (meta-design: ops-manager / meta-scheduling: ops-manager / meta-decision: coo). 4 load-bearing release-discipline layers (pre-release scaffold + plan-critic + pre-execution external-API validation + post-ExitPlanMode pressure-test). Next likely candidates: cost-aware critic culling, pre-summary skill, design-subdept, persistent in-flight deliberation state.
