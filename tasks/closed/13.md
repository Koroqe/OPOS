---
issue_number: 13
title: v0.6.0 — Scheduled processes mechanism
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-05-30
completed: 2026-05-30
success_criteria:
  - ui/scheduling.py validator module shipped + importable + 8 unittest fixtures PASS
  - shared/templates/scheduled-run.md.tmpl shipped + 11-field schema parses
  - 3 new skill folders (schedule-process, unschedule-process, list-scheduled-processes) each with SKILL.md + PROCESS.md + history/.gitkeep + scheduled-runs/.gitkeep
  - ops-manager owns_processes extended from 3 to 6
  - PROCESS.md.tmpl has 4 commented-out scheduling frontmatter fields + Scheduled runs body section
  - design-process surfaces scheduled as 4th trigger option + creates scheduled-runs/.gitkeep eagerly for scheduled processes
  - CLAUDE.md.jinja addendum + copier.yml + .gitignore + MAINTAINER.md + README.md.jinja Scheduling-processes section + RISKS.md.jinja 9 new risks + CHANGELOG ## [0.6.0] entry shipped
  - v0.6.0 GitHub release tag exists; post-release scaffold ships all deliverables and excludes runtime entries
  - ≥ 37 unittest PASS; 16/16 smoke PASS
  - issue #13 CLOSED with status:done; tasks/13.md archived to tasks/closed/13.md; .current-task absent
deadline:
related_skills: [task-register, task-update, task-complete, design-process, release-from-changelog]
---

# v0.6.0 — Scheduled processes mechanism

## Goal

Ship OPOS v0.6.0: the **scheduling mechanism** that lets any process fire on a cron schedule. Adds 4 frontmatter fields to PROCESS.md, 3 new ops-manager-owned skills (schedule-process / unschedule-process / list-scheduled-processes), a scheduled-runs/ folder convention, and a ui/scheduling.py validator module. Wraps Claude Code's built-in cron tools (CronCreate / CronList / CronDelete). v1 enforces authority: as a declared contract via prelude injection + in-band self-check; post-run sandbox guard deferred to v2.

## Acceptance criteria

See issue body's "Acceptance criteria" section (11 checks).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

**Shipped:** v0.6.0 — Scheduled processes mechanism. **First release where OPOS can run unattended.** 3 new ops-manager-owned skills (schedule-process / unschedule-process / list-scheduled-processes) wrapping Claude Code's CronCreate / CronList / CronDelete. ui/scheduling.py validator + 11 unittest fixtures. scheduled-run.md.tmpl + 11-field schema. PROCESS.md.tmpl gains 4 optional scheduling frontmatter fields + Scheduled runs body section. design-process surfaces "scheduled" as 4th trigger option. ops-manager.md owns_processes extended 3 → 6. Released at https://github.com/Koroqe/OPOS/releases/tag/v0.6.0. Issue closed via task-complete; final comment at https://github.com/Koroqe/OPOS/issues/13#issuecomment-4585076722.

**Commits:** 9 in v0.5.3..HEAD range, all with `Refs: #13` (100% adoption — 13 consecutive releases at 100%).

**Verification:**
- Pre-release scaffold check (v0.3.1 step 5): exit 0 in ~5s — 7 consecutive clean releases.
- Post-release scaffold ships all v0.6.0 deliverables; ZERO runtime files (scheduled-runs/202*.md excluded; .claude/scheduled-processes.json excluded).
- 53 unittest PASS (was 37 before v0.6.0; +16 new from Slices 1 + 2).
- 16/16 smoke PASS.
- All 11 acceptance criteria met.

**Plan critic + new convention:** Plan critic load-bearing for 6 consecutive releases (v0.4.0 through v0.6.0). NEW v0.6.0 convention: pre-execution tool-name validation via claude-code-guide agent caught brainstorm plan's wrong tool names (`create_scheduled_task` → `CronCreate`, etc.) BEFORE any wrapper SKILL.md was written. This is now the 3rd load-bearing release-discipline pillar after plan-critic and pre-release scaffold check.

**Deviations from plan:** Slice 7 (PRD/use-cases/qa-docs) dropped per user choice (Doc-phases deviation acknowledged — matches prior 12 OPOS releases). 10 use cases captured as condensed README narrative + RISKS new entries + CHANGELOG Notes. Slice numbering shifted from brainstorm's 1-8 to 0-9 (added task-register + release + task-complete wrappers per OPOS release cadence). Tool names corrected to CronCreate / CronList / CronDelete throughout.

**Framework state after v0.6.0:** autonomous-capable. ops-manager owns 6 skills (3 meta-design + 3 meta-scheduling) and is the framework's single owner of both self-extension axes (WHAT processes exist + WHEN they run). Next likely candidates: post-run sandbox guard for authority enforcement (Risk 18, v2), health-check skill for silent-cron-failure detection (Risk 20, v2), design-subdept (RISKS Risk 8 fully-closed at top level only), data/+backlog/ backfill on 5 starters, canonical framework-reserved-names.md.
