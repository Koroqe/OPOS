---
issue_number: 7
title: v0.3.1 patch — console UX fixes + v0.3.0 carryover
owner: chief-of-staff
depts: [engineering, company]
state: completed
created: 2026-05-28
completed: 2026-05-29
success_criteria:
  - /skills/deploy returns 200; /departments/engineering lists deploy under dept-scoped skills
  - Activity feed within a day sorts by time when present, falls back to run_id otherwise
  - release-from-changelog SKILL.md has a pre_release_scaffold_check step
  - Dashboard Departments tile relabeled; zero-count tile links visually distinguished
  - v0.3.1 released; no breaking changes
  - All v0.3.x consumers copier update cleanly
deadline:
related_skills: [task-register, task-update, task-complete, release-from-changelog, serve-console]
---

# v0.3.1 patch — console UX fixes + v0.3.0 carryover

## Goal

Patch release closing the v0.3.0 console UX warts (parse_skills missing dept-nested; activity sort opacity; dashboard polish) and folding the v0.3.0 release-process carryover (pre-release scaffold check) into release-from-changelog.

## Acceptance criteria

(See frontmatter `success_criteria:` above — 6 items.)

## Progress log

- 2026-05-28 — task-register — issue #7 opened; first run of the v0.3.0-FIXED task-register SKILL.md (used `gh issue create` without --json + `basename` URL parse).

## Final outcome

Shipped v0.3.1 on 2026-05-29. All 6 acceptance criteria satisfied. 7 commits all carrying `Refs: #7`; v0.3.1 release at https://github.com/Koroqe/OPOS/releases/tag/v0.3.1; final comment at https://github.com/Koroqe/OPOS/issues/7#issuecomment-4569049769. Issue closed CLOSED/COMPLETED with `status:done` label.

**Highlights:**
- First clean release cut via `release-from-changelog` since the pre-release scaffold check was added — no destructive recovery needed (compared to v0.3.0 which required delete-and-re-cut).
- The fix exercised: parse_skills now finds 12 skills (was 11) including dept-nested `deploy`.
- First entry to use the new optional `time:` history field (recursive dogfood).
- Test coverage: 30 → 34 unittest cases.

**Pending for v0.4.0:** historical-issue blind spot (lazy backfill from gh), inline proposed_delta on skill page, live session tailing, design-agent skill.
