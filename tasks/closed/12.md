---
issue_number: 12
title: v0.5.3 — design-department skill
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-05-30
completed: 2026-05-30
success_criteria:
  - design-department skill present with verbatim regex `^[a-z][a-z0-9-]{0,63}$`, 12 steps under `## Steps`, framework-reserved name list, context-detected charter suffix
  - ops-manager `owns_processes` extended to 3 design-family skills
  - RISKS Risk 8 marked FULLY CLOSED in v0.5.3
  - design-process Failure modes hand-off updated
  - README "How to use OPOS day-to-day" has 5 example user prompts
  - chief-of-staff Framework expertise reflects 16 skills (15 → 16; design-department marked NEW v0.5.3)
  - CHANGELOG `## [0.5.3]` entry + link reference shipped
  - v0.5.3 GitHub release tag exists; post-release scaffold ships all v0.5.3 deliverables
  - 37/37 unittest PASS; 16/16 smoke PASS
  - issue #12 CLOSED with status:done; tasks/12.md archived to tasks/closed/12.md; .current-task absent
deadline:
related_skills: [task-register, task-update, task-complete, design-department, release-from-changelog]
---

# v0.5.3 — design-department skill

## Goal

Ship OPOS v0.5.3: the `design-department` skill — third member of the `design-*` family. Closes the final org-chart-expansion gap that has been deferred across 5 releases (RISKS Risk 8 partial-closed since v0.4.0, fully-closed by v0.5.3). With v0.5.1 founders can CUSTOMIZE the 6 starter depts via `/company-setup`; with v0.5.3 they can ADD new top-level depts via `/design-department <description>` — same interactive consult → propose → iterate → approval pattern as `design-process` and `design-agent`. Owner: `ops-manager`. Optionally emits a `/design-agent` recommendation in the same session for the new dept's lead.

## Acceptance criteria

See issue body's "Acceptance criteria" section (11 checks: 10 grep/exec checks + the post-release scaffold check + the test-suite checks).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

**Shipped:** v0.5.3 — `design-department` skill (third member of design-* family). Closes RISKS Risk 8 fully. ops-manager owns all 3 design-family skills. Released at https://github.com/Koroqe/OPOS/releases/tag/v0.5.3. Issue closed via task-complete; final comment at https://github.com/Koroqe/OPOS/issues/12#issuecomment-4583054523.

**Commits:** 6 in v0.5.2..HEAD range, all with `Refs: #12` (100% adoption).

**Verification:**
- Pre-release scaffold check (v0.3.1 step 5): exit 0 in ~5s.
- Post-release scaffold ships all 4 v0.5.3 deliverables.
- 37/37 unittest + 16/16 smoke PASS.
- 11 acceptance criteria all met.

**Plan critic:** 13 findings (0 critical, 5 major, 8 minor); all CRITICAL/MAJOR addressed in-plan before execution. Plan-critic load-bearing for 5 consecutive releases.

**Deviations from plan:** none. 7 slices executed cleanly; Slices 2 + 3 ran sequentially under single-agent execution but were marked Wave 2 parallel-safe in the plan (file disjoint — no conflict possible).

**Framework state after v0.5.3:** the full design-* family is operational. ops-manager can generate any framework primitive (skill / agent / dept) from natural-language input. Next likely candidates (not in v0.5.3 scope): `design-subdept` (sub-depts under existing depts), `data/` + `backlog/` backfill on 6 starters, or a canonical `framework-reserved-names.md`.
