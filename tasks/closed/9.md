---
issue_number: 9
title: v0.5.0 — company-setup skill (founder onboarding)
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-05-29
completed: 2026-05-29
success_criteria:
  - company-setup skill ships with 10-step procedure + tools allow-list + state_schema
  - POLICY.md.tmpl ships with 7 tokens, no version field
  - copier.yml has root CLAUDE.md in _skip_if_exists
  - coo agent updated (owns_processes + Bash tool + description)
  - README has First steps after scaffold section
  - 37 unittest + 16 smoke + 3 title-heuristic all green
  - v0.5.0 released; no breaking changes
  - Post-release scaffold ships company-setup + POLICY.md.tmpl
deadline:
related_skills: [task-register, task-update, task-complete, release-from-changelog, company-setup, design-process]
---

# v0.5.0 — company-setup skill (founder onboarding)

## Goal

Ship the `company-setup` skill that gives founders a smooth conversational onboarding from a fresh OPOS scaffold to a populated company OS. Closes the 6 founder-blank gaps (Mission, Values, strategic priorities, dept missions, policies folder). First real consumer use: the user's `zipread` company.

## Acceptance criteria

(See frontmatter `success_criteria:` — 8 items.)

## Progress log

- 2026-05-29 — task-register — issue #9 opened; v0.3.0-FIXED task-register pattern; `dept:company` label only (no engineering for this purely-company-scope work).

## Final outcome

Shipped v0.5.0 on 2026-05-29. All 8 acceptance criteria satisfied. 7 commits with `Refs: #9` + 1 between-release side task (`c142a75` kb-curator dogfood) — flagged in final comment. v0.5.0 release at https://github.com/Koroqe/OPOS/releases/tag/v0.5.0; final comment at https://github.com/Koroqe/OPOS/issues/9#issuecomment-4576997280. Issue closed CLOSED/COMPLETED with `status:done` label.

**Highlights:**
- First consumer-facing skill in the framework (vs prior framework-internal additions).
- CRITICAL plan-critic finding (root CLAUDE.md → `_skip_if_exists`) was caught BEFORE founder use — without this, every `copier update` would have created `.rej` conflicts on Mission/Values.
- Third consecutive clean release (v0.3.1, v0.4.0, v0.5.0); the v0.3.1 pre-release scaffold check is now load-bearing infrastructure.
- Post-release scaffold verification ships all v0.5.0 deliverables; consumer's `/skills/company-setup` returns 200.

**Pending for v0.5.1+ (logged for the next release):**
- kb-curator dogfood `proposed_delta`: document conflict-resolution heuristic in design-agent + ship `.gitkeep` + README when an agent introduces a new folder convention.
- First-real-founder run feedback from the user's `zipread` setup (highest-signal source for v0.5.1).
- `design-department` skill (remaining org-chart-expansion gap).
- Live Claude Code session tailing (deferred from v0.4.0 alternatives).
