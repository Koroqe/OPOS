---
issue_number: 6
title: Ship v0.3.0 — local-host read-only console UI
owner: chief-of-staff
depts: [engineering, company]
state: completed
created: 2026-05-28
completed: 2026-05-28
success_criteria:
  - 5 console pages live (dashboard, tasks, agents, skills, activity) plus 4 detail variants
  - 10 routes return 200; bad-input routes return 400 (path-traversal blocked at validator)
  - serve-console skill works (python3 ui/console.py boots a working console)
  - markdown and jinja2 deps documented; missing-dep produces a clear install hint
  - release-from-changelog title heuristic fixed and PRE-VALIDATED via ui/tests/test_title_heuristic.sh
  - v0.3.0 released via release-from-changelog (self-test of the fixed heuristic)
  - copier copy smoke test passes; consumer scaffold receives UI but not framework's dogfooded tests
  - No breaking changes for v0.2.x consumers
deadline:
related_skills: [task-register, task-update, task-complete, release-from-changelog, serve-console]
---

# Ship v0.3.0 — local-host read-only console UI

## Goal

Ship v0.3.0 of OPOS: a local-host read-only console UI giving humans visibility into framework state without going through Claude Code or `gh`. Built with Python http.server + Jinja2; one new optional Python dep (`markdown`). Adds a new `ui/` top-level directory and a `serve-console` skill. Also folds in the title-derivation fix surfaced as a `proposed_delta` from v0.2.0's `release-from-changelog` history entry.

## Acceptance criteria

(See frontmatter `success_criteria:` above — 8 items.)

## Progress log

- 2026-05-28 — task-register — issue #6 opened; all 3 labels pre-existed (no creation needed); private repo (no warning); plan file: `~/.claude/plans/company-os-framework-jaunty-sonnet.md`.

## Final outcome

Shipped v0.3.0 on 2026-05-28. All 8 acceptance criteria satisfied. 12 commits all carrying `Refs: #6`; v0.3.0 release at https://github.com/Koroqe/OPOS/releases/tag/v0.3.0; final comment at https://github.com/Koroqe/OPOS/issues/6#issuecomment-4568682841. Issue closed CLOSED/COMPLETED with `status:done` label.

**Deviations:** the live release was cut twice (delete + re-cut) because the first cut tagged at the Slice 9 commit BEFORE the Slice 10 rename of `ui/templates/*.html.jinja` → `*.html` was discovered to be necessary (Copier was choking on the `.jinja` suffix when scaffolding a consumer). Documented in `.claude/skills/release-from-changelog/history/2026-05-28-v030.md` `proposed_delta` — suggests adding a `pre_release_scaffold_check` step to `release-from-changelog/SKILL.md` for v0.4.0.

**Surfaced bugs (fixed during this task):**
1. `task-register/SKILL.md` step 9 incorrectly documented `gh issue create --json url,number` — that flag is not supported by `gh issue create`. Fixed in-flight (Slice 9) via Rule 3 auto-resolve.
2. `release-from-changelog/SKILL.md` step 5 derived `v0.2.0 — Added` because the heuristic didn't skip Keep-a-Changelog reserved section names. Fixed in Slice 8; PRE-validated by `ui/tests/test_title_heuristic.sh`; live-validated by this release.
3. `ui/templates/*.html.jinja` filenames were processed by Copier as templates instead of being shipped verbatim. Fixed in Slice 10 rename.

**Pending for v0.4.0** (collected from `proposed_delta`s of skill runs during this task):
- `pre_release_scaffold_check` step in `release-from-changelog` (would have caught the rename issue without destructive re-cut)
- Live Claude Code session tailing (read JSONL files under `~/.claude/projects/`)
- Shared `_derive_title()` shell function for `release-from-changelog` SKILL.md + test (avoid drift)
- Retroactive `tasks/<n>.md` generator for pre-v0.2.0 issues
- `task-register` step 9 documented fix is now codified, but the `--json` removal could be the trigger to also adopt `--body-file` everywhere (currently SKILL.md still shows `--body "$RENDERED"` in some examples)
