---
issue_number: 17
title: v0.7.2 — pipeline hygiene bundle (dev-deps + step-14 fix + smoke CSS assertions)
owner: chief-of-staff
depts: [company]
state: completed
created: 2026-06-25
completed: 2026-06-25
success_criteria:
  - requirements-dev.txt pinned ranges for markdown/copier/pyyaml/jinja2; ships as CORE
  - MAINTAINER.md "Testing locally" gains dev-deps prereq subsection
  - task-complete step 14 + task-pause step 5 use Python one-liner (no flaky grep-v && mv chain remaining)
  - v0.6.1 git mv preserved at task-complete step 13
  - ui/smoke.sh has 7 dept + 1 fallback CSS rule assertions; 24/24 PASS
  - release-from-changelog step 5 auto-runs pip install -r requirements-dev.txt with warn-and-continue
  - CHANGELOG ## [0.7.2] + link reference shipped; RISKS Risk 30 status update
  - v0.7.2 GitHub release tag exists
  - 53/53 unittest + 24/24 smoke PASS
  - Issue #17 CLOSED with status:done; tasks/17.md archived via git mv; .current-task absent OR without 17
  - CRITICAL VALIDATION (Slice 7) — step 14 works on FIRST INVOCATION (no retry)
deadline:
related_skills: [task-register, task-complete, task-pause, release-from-changelog]
---

# v0.7.2 — pipeline hygiene bundle (dev-deps + step-14 fix + smoke CSS assertions)

## Goal

Patch release addressing 3 pipeline-quality issues at root: (1) ship `requirements-dev.txt` to pin Python dev-deps (resolves the 2-release skipped-scaffold-check pattern from v0.7.0 + v0.7.1); (2) rewrite `task-complete` step 14 + `task-pause` step 5 with Python one-liner (eliminates the reproducibly-flaky `grep -v ... && mv ...` shell chain that surfaced in v0.7.0 Slice 10 + v0.7.1 Slice 4); (3) add 8 dept-badge CSS rule assertions to `ui/smoke.sh` (catches the v0.7.1 white-on-white bug class). Plus `release-from-changelog` step 5 auto-installs the new pinned deps. Slice 7 is the validating exercise — first task-complete under the new Python step-14 must work on first invocation.

## Acceptance criteria

See issue body's "Acceptance criteria" section (15 checks, including the CRITICAL VALIDATION at Slice 7).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

**Shipped:** v0.7.2 — pipeline hygiene bundle. 3 sub-items + 1 glue piece, all addressed at root. Released at https://github.com/Koroqe/OPOS/releases/tag/v0.7.2. Issue closed via task-complete; final comment at https://github.com/Koroqe/OPOS/issues/17#issuecomment-4798639787.

### 🎉 CRITICAL VALIDATION RESULT — Python step-14 fix WORKED ON FIRST INVOCATION

THIS task-complete run was the validating exercise of v0.7.2's core Python step-14 fix.

**Outcome:** The Python one-liner cleared `.current-task` cleanly on first invocation. **No retry needed.** Trace:
- Before: `.current-task` = `17` (single-element array)
- Python exit: 0
- After: `.current-task` ABSENT (single-element case → `lines = []` branch → `os.remove(target)` fired)

**This is the first task-complete run in v0.7.x where step 14 worked first-try.** v0.7.0 Slice 10 + v0.7.1 Slice 4 both required manual retry of the (now-replaced) shell chain. The Python one-liner replacement eliminated the reproducible anomaly AT ROOT — exactly what Slice 2's design intended.

### Self-improving framework demonstrated TWICE this release

1. **Slice 4 → Slice 6:** Slice 4 shipped `pip install --quiet -r requirements-dev.txt` as a release-from-changelog step-5 prefix; Slice 6 was the first release-from-changelog run to benefit — pre-release scaffold check ran successfully for the first time in v0.7.x (was skipped in v0.7.0 + v0.7.1).
2. **Slice 2 → Slice 7:** Slice 2 rewrote task-complete step 14 with Python; Slice 7 (THIS run) was the first task-complete to use it — worked on first invocation.

The release pipeline fixes its own recurring failure modes in the same release. Pipeline maturity confirmed.

### Verification

- Pre-release scaffold check: ran successfully (`pip install` exit 0 → `copier copy` exit 0). First in v0.7.x.
- Post-release scaffold ships all consumer-facing deliverables.
- 53/53 unittest + **24/24 smoke** PASS (was 16/16; +8 new dept-badge CSS rule assertions caught the v0.7.1 bug class).
- 7 v0.7.2 commits, all `Refs: #17` (100% adoption — 17 consecutive releases at 100%).
- All 15 acceptance criteria met (including the CRITICAL VALIDATION).

### Framework state after v0.7.2

The 3 pipeline-quality issues that have been compounding across v0.7.x are all RESOLVED AT ROOT:

| Issue | Resolution |
|---|---|
| Fresh-machine env regressions skipping scaffold check (v0.7.0 + v0.7.1) | `requirements-dev.txt` + Slice 4 auto-pip-install |
| `task-complete` step 14 + `task-pause` step 5 grep-v shell chain reproducibly flaky | Python one-liner (validated this run) |
| CSS rendering quality not asserted (v0.7.1 white-on-white bug shipped invisibly) | 8 new smoke assertions; 16/16 → 24/24 |

Plan-critic + post-ExitPlanMode pressure-test load-bearing count remains at 6 consecutive releases (v0.4.0 → v0.6.1) — v0.7.0 + v0.7.1 + v0.7.2 all skipped both layers. Trade-off acknowledged; re-establish for v0.8.0+ substantive releases.

Next candidates: cross-machine state coordination (Risk 15 deferred half); `flock`-based locking around `.current-task` `>>` append (Risk 30 ORIGINAL surface); cost-aware critic culling for deliberate-decision (Risk 27); pre-summary skill (Risk 28); `design-subdept`; canonical `framework-reserved-names.md`; visual regression with headless-browser screenshot diff (extends Slice 3's smoke CSS assertions).
