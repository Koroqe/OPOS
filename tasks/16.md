---
issue_number: 16
title: v0.7.1 — fix console dept-badge white-on-white rendering
owner: chief-of-staff
depts: [company]
state: active
created: 2026-06-12
completed:
success_criteria:
  - ui/static/console.css has 5 new dept rules (finance, people, legal, commercial, pr) + fallback `background: var(--fg-dim)` on base .dept[data-dept] rule
  - legacy dept rules (engineering, sales, marketing, ops) preserved for backwards-compat
  - CHANGELOG ## [0.7.1] entry + link reference shipped
  - v0.7.1 GitHub release tag exists
  - 53/53 unittest PASS; 16/16 smoke PASS
  - Issue #16 CLOSED with status:done; tasks/16.md archived via git mv
deadline:
related_skills: [task-register, task-complete, release-from-changelog]
---

# v0.7.1 — fix console dept-badge white-on-white rendering

## Goal

Patch fix for the console UI: `ui/static/console.css` line 129 sets `color: white` for all `.dept[data-dept]` badges, but the per-dept background-color rules only cover the stale pre-v0.5.1 dept names. 5 of 7 starter depts (finance, people, legal, commercial, pr) fall through → white text on white background → invisible. Discovered by user during live console review of v0.7.0. Add the 5 missing dept mappings + a fallback `background: var(--fg-dim)` so future user-created depts (via `/design-department`) also get a sensible default rather than invisible.

## Acceptance criteria

See issue body's "Acceptance criteria" section (10 checks).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

(Written by `task-complete` at task close.)
