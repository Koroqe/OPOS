---
issue_number: 13
title: v0.6.0 — Scheduled processes mechanism
owner: chief-of-staff
depts: [company]
state: active
created: 2026-05-30
completed:
success_criteria:
  - ui/scheduling.py validator module shipped + importable + 8 unittest fixtures PASS
  - shared/templates/scheduled-run.md.tmpl shipped + 11-field schema parses
  - 3 new skill folders (schedule-process, unschedule-process, list-scheduled-processes) each with SKILL.md + PROCESS.md + history/.gitkeep + scheduled-runs/.gitkeep
  - ops-manager owns_processes extended from 3 to 6
  - PROCESS.md.tmpl has 4 commented-out scheduling frontmatter fields + Scheduled runs body section
  - design-process surfaces scheduled as 4th trigger option + creates scheduled-runs/.gitkeep eagerly for scheduled processes
  - CLAUDE.md.jinja addendum + copier.yml + .gitignore + MAINTAINER.md + README.md.jinja Scheduling-processes section + RISKS.md.jinja 9 new risks + CHANGELOG ## [0.6.0] entry shipped
  - v0.6.0 GitHub release tag exists; post-release scaffold ships all deliverables and excludes runtime entries
  - ≥ 37 unittest PASS; 16/16 smoke PASS
  - issue #13 CLOSED with status:done; tasks/13.md archived to tasks/closed/13.md; .current-task absent
deadline:
related_skills: [task-register, task-update, task-complete, design-process, release-from-changelog]
---

# v0.6.0 — Scheduled processes mechanism

## Goal

Ship OPOS v0.6.0: the **scheduling mechanism** that lets any process fire on a cron schedule. Adds 4 frontmatter fields to PROCESS.md, 3 new ops-manager-owned skills (schedule-process / unschedule-process / list-scheduled-processes), a scheduled-runs/ folder convention, and a ui/scheduling.py validator module. Wraps Claude Code's built-in cron tools (CronCreate / CronList / CronDelete). v1 enforces authority: as a declared contract via prelude injection + in-band self-check; post-run sandbox guard deferred to v2.

## Acceptance criteria

See issue body's "Acceptance criteria" section (11 checks).

## Progress log

(Appended by `task-update` invocations.)

## Final outcome

(Written by `task-complete` at task close.)
