---
issue_number: 6
title: Ship v0.3.0 — local-host read-only console UI
owner: chief-of-staff
depts: [engineering, company]
state: active
created: 2026-05-28
completed:
success_criteria:
  - 5 console pages live (dashboard, tasks, agents, skills, activity) plus 4 detail variants
  - 10 routes return 200; bad-input routes return 400 (path-traversal blocked at validator)
  - serve-console skill works (python3 ui/console.py boots a working console)
  - markdown and jinja2 deps documented; missing-dep produces a clear install hint
  - release-from-changelog title heuristic fixed and PRE-VALIDATED via ui/tests/test_title_heuristic.sh
  - v0.3.0 released via release-from-changelog (self-test of the fixed heuristic)
  - copier copy smoke test passes; consumer scaffold receives UI but not framework's dogfooded tests
  - No breaking changes for v0.2.x consumers
deadline:
related_skills: [task-register, task-update, task-complete, release-from-changelog, serve-console]
---

# Ship v0.3.0 — local-host read-only console UI

## Goal

Ship v0.3.0 of OPOS: a local-host read-only console UI giving humans visibility into framework state without going through Claude Code or `gh`. Built with Python http.server + Jinja2; one new optional Python dep (`markdown`). Adds a new `ui/` top-level directory and a `serve-console` skill. Also folds in the title-derivation fix surfaced as a `proposed_delta` from v0.2.0's `release-from-changelog` history entry.

## Acceptance criteria

(See frontmatter `success_criteria:` above — 8 items.)

## Progress log

- 2026-05-28 — task-register — issue #6 opened; all 3 labels pre-existed (no creation needed); private repo (no warning); plan file: `~/.claude/plans/company-os-framework-jaunty-sonnet.md`.

## Final outcome

(Written by `task-complete` at task close.)
