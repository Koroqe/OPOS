# Resource registry

Index of everything this company's agents can reach. One row per resource file in this folder (schema + rules: [`README.md`](./README.md)). Sessions surface this at first touch; the design family derives agent `tools:` grants from it; `propose-to-core` builds its redaction blocklist from it.

| resource | kind | owner_human | machines | status |
|---|---|---|---|---|
